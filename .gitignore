# Secrets — never commit. The generator reads the API key from here.
.env
.env.*
!.env.example

_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
vendor/
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# Strategy documents live outside the repo (D:\SEO COINCONNECT\strategy\).
# The repo is public; the content plan and keyword targets are not.
pvara-content-calendar-phase1.md
CONTENT-PROMPT.md
